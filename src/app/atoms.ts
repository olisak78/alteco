// Atom definitions - global state variables

import { atom } from 'jotai';

export const atomStatus = atom(0);
export const atomFilterType = atom('Filter by');
export const atomFilterText = atom('');
